<h1>Listing comments</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.entry %></td>
    <td><%= link_to 'Show', @picture, comment %></td>
    <td><%= link_to 'Edit', edit_picture_comment_path(@picture, comment) %></td>
    <td><%= link_to 'Destroy', [@picture, comment], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment', new_comment_path %>
